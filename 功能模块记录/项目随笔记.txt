站点信息，保存在php生成的一个静态页面。

阅读数，使用ajax。

使用layer插件，实现弹出层。封装dialog.js，适合用于本cms的类方法。

异步登录，ajax。而不是跳转。

apache本地设置虚拟域名的方式，将虚拟域名绑定到您的项目根目录


重定向到指定的URL地址  redirect('/admin.php')
重定向到新的模块      $this->redirect('New/category')重定向到New模块的Category操作

Model层方法，首先检验参数数据的合法性。例如id,是否为空，是否为数字。不合法抛出异常。
改进：Model层方法，检验参数数据的合法性。检验是否为空，不能用!id,这样当id为0时会被检测为不合法。

改动：排序功能， 排序成功后，弹层跳转url,php返回前一页面url。出现问题：页码变成负数，导致操作数据库失败。改进为：js本地设置为当前url(起刷新功能)。

